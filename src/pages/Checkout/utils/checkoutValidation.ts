/**
 * Validações do Checkout
 * 
 * @deprecated Este arquivo está sendo migrado para usar os schemas centralizados.
 * Use {@link import('@/lib/validation/schemas')} para novas implementações.
 * Mantido para compatibilidade com código existente.
 */
export {
  emailSchema,
  whatsappSchema,
  validateEmail,
  validateWhatsapp,
  formatWhatsappForCakto,
} from '@/lib/validation/schemas';

